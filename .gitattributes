# 统一行尾为 LF。
#
# 背景：仓库长期在 Windows 上开发，Git 的 core.autocrlf 会把检出文件转成 CRLF、
# 提交时再转回 LF，导致 biome 报出大量「行尾不一致」的噪音（曾累计 314 条），
# 淹没真正的问题。这里显式声明，让两端行为一致、评审时不再被格式化噪音干扰。
* text=auto eol=lf

# 图片等二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.wasm binary
*.woff binary
*.woff2 binary

# Windows 专用脚本保留 CRLF（若将来重新引入 .bat/.ps1）
*.bat text eol=crlf
*.ps1 text eol=crlf

# 锁文件默认是按行对比噪音较大
package-lock.json -diff linguist-generated
